{% apply text_email %}
    <twig:block name="body">{{ '' }}</twig:block>

   <twig:block name="signature">
       Cordialement,
       L'équipe {{ devscast_dashlite.application.name }}.
   </twig:block>
{% endapply %}
