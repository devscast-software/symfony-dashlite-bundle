<a {% for attr, value  in options %} {{ attr }}="{{ value }}"{% endfor %}>
<span class="float-right">
    {% if sorted %}
        {% if direction == 'desc' %}
            <twig:Dashlite:Icon name="arrow-up"/>
        {% else %}
            <twig:Dashlite:Icon name="arrow-down"/>
        {% endif %}
    {% else %}
        <twig:Dashlite:Icon name="opt-alt"/>
    {% endif %}
</span>
{{ title }}
</a>
